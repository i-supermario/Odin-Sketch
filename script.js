function createGrid(rows,cols){

}

function selectGridSize(evt){
    let gridRows = prompt("No of grid rows?");
    let gridCols = prompt("No of grid columns?");
    function createGrid(gridRows,gridCols);
}

gridBtn = document.querySelector('.select');
gridBtn.addEventListener('click',(evt) => selectGridSize(evt));
